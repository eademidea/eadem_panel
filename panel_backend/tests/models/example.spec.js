var example = require('../../models/example')

describe('Testes na camada de banco de dados...', () => {
    it('Teste exemplo', () => {
        const result = true;
        expect(result).toBe(true)
    })
})